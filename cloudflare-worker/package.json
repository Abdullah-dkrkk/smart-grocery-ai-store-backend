{
  "name": "product-image-generator",
  "version": "1.0.0",
  "description": "Cloudflare Worker for generating product variation images using Pollination.ai",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
